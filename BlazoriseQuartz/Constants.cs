using System;
namespace BlazoriseQuartz
{
    public enum BasePage
    {
        Overview,
        Schedules,
        Triggers,
        History,
        Calendars,
        None
    }

    public enum ScheduleDialogTab
    {
        Job,
        Trigger
    }
}

